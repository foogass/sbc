<!DOCTYPE html>
<html>
<head>
	
</head>
<body>
	<h2>записаться</h2>

	<script type="text/javascript">

		$(function() {
			//тут пишем Java-script код, который исполнится после загрузки страницы
			$('#datetimepicker').datetimepicker();

			});

	</script>

	<form action="/visit" method="POST" class="form-horizontal">
		<div class="form-group">
		    <label class="col-sm-2 control-label">Ваше имя</label>
		    <div class="col-sm-10">
		      <input name="username" type="text" class="form-control" placeholder="name" value="<%= @username %>">
			</div>
		</div>

		<div class="form-group">
		    <label class="col-sm-2 control-label">Ваш телефон</label>
		    <div class="col-sm-10">
		      <input name="phone" type="text" class="form-control" placeholder="phone" value="<%= @phone %>">
		    </div>
		</div>

		<div class="form-group">
		    <label class="col-sm-2 control-label">Когда вас ждать?</label>
		    <div class="col-sm-10">
		      <input id="datetimepicker" name="date_time" type="text" class="form-control" placeholder="date and time" value="<%= @date_time %>">
		    </div>
		</div>

		<div class="form-group">
			<label class="col-sm-2 control-label">Выберите парикмахера: </label>
			<div class="col-sm-10">
				<select name="barber" class="form-control">
					<% @barget.each do |row| %>
				  		<option <%= @barber == row['name'] ? 'selected' : '' %>><%= row['name'] %></option>

					<% end %>
				</select>
			</div>
		</div>

		<div class="form-group">
		    <div class="col-sm-offset-2 col-sm-10">
		      <button type="submit" class="btn btn-default">Записаться</button>
		    </div>
		</div>
	</form>

</body>
</html>
